- **gestion-de-carritos**: User pointed out the placeholder images in the dashboard ("Vehículos Recientes") were completely random and didn't make sense (e.g., toy trains for a firetruck).
- Generated new 3D render placeholder images using Gemini 3 Pro Image (`nano-banana-pro` skill) for the firetruck, safari train, pony, and police motorcycle.
- **gestion-de-carritos Theme Refactor**: User requested a clean, professional, light control panel theme ("control theme but not black") instead of the hardcoded dark hex codes (`#111111`, `#0a0a0a`). Implemented a light modern admin theme (slate backgrounds, white cards, indigo accents) replacing all hardcoded colors across the Next.js app.
- **gestion-de-carritos Obys Agency Theme**: The user later shared a link to Obys Agency's library (`library.obys.agency`) and asked to get inspired by it. Refactored the entire theme again to an "Obys Agency Inspired Theme - Brutalist Elegant" (beige/off-white background `#e8e6e1`, very dark gray `#111111`, brutalist red accent `#c02c25`, massive typography, tight tracking, uppercase headers, hard borders, and grayscale images that colorize slowly on hover).
- **gestion-de-carritos Real Photos**: The user started sending real photos of the kid rides taken from a Timemark inspection app from various malls (Mi Centro El Porvenir, Mercurio Soacha, Milenio Plaza). Cropped the raw photos locally using `ffmpeg`, saved them to `public/images/`, and updated the `mockData.ts` database with the real vehicles (Rayo McQueen, Helicóptero Espacial, Jeep Safari, Moto Chopper, Avión Azul, Moto 95).
- **gestion-de-carritos MongoDB Fix**: The `/admin` page was broken on production (Netlify) with a `ENOTFOUND _mongodb._tcp.cluster0.mongodb.net` error because the Netlify deployment was missing the `DATABASE_URL` environment variable. I injected the environment variable to Netlify using the CLI, but also provided a "mock/standalone" version of the admin panel so it would load properly without crashing. Reverted to the MongoDB-connected version after setting the env var, but later went back to the standalone mock version since the MongoDB connection was still failing for the user (the connection string was just a placeholder). Instructed the user to create a real MongoDB Atlas cluster so we can connect it later.
